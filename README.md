# evolver
Evolver v3.1 is an autonomous Chrome extension that automates EVOLVE E-LEARNING platform. It parses the DOM, sends questions to local (Ollama) or cloud LLMs (OpenAI, Gemini), clicks the correct answers, and auto-navigates pages until the exam finishes. A lightweight, self-driving browser agent for web-form automation.
